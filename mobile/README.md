移动端（Expo）快速启动：
1. 安装 Node.js & Expo CLI: npm install -g expo-cli
2. 进入 mobile 目录: npm install
3. 启动: expo start
4. 用手机扫码或模拟器打开。请在 App.js 中设置 BACKEND_URL（默认 http://10.0.2.2:8000 或 http://localhost:8000，Android 模拟器需调整）。
